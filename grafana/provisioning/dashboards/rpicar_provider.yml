apiVersion: 1
providers:
  - name: RPiCar
    orgId: 1
    folder: RPi Car
    folderUid: rpicar-folder
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
