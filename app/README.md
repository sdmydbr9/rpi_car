# RPi Car Control - Mobile App

This is a React Native mobile application for controlling the RPi Car, supporting both Android and iOS platforms.

## Project Structure

```
app/
├── android/          # Android native project
├── ios/              # iOS native project
├── src/
│   ├── components/   # Reusable components
│   ├── screens/      # Screen components
│   ├── lib/          # Utility libraries
│   └── hooks/        # Custom React hooks
├── App.tsx           # Main app component
└── package.json      # Dependencies
```

## Prerequisites

- Node.js (v14 or higher)
- npm or yarn
- Expo CLI
- For Android development: Android Studio
- For iOS development: Xcode (macOS only)

## Installation

1. Navigate to the app directory:
```bash
cd app
```

2. Install dependencies:
```bash
npm install
```

## Running the App

### Development Mode

Start the Expo development server:
```bash
npm start
```

### Android

Run on Android device or emulator:
```bash
npm run android
```

### iOS

Run on iOS device or simulator (macOS only):
```bash
npm run ios
```

### Web

Run in web browser (for testing):
```bash
npm run web
```

## Building for Production

### Android APK

```bash
npx expo build:android
```

### iOS App

```bash
npx expo build:ios
```

## Features

- Real-time connection to RPi Car backend via WebSocket
- Vehicle control interface:
  - Steering control (left, center, right)
  - Throttle and brake pedals
  - Gear shifter (R, N, 1, 2, 3)
  - Emergency stop button
- Live telemetry display:
  - Speed
  - RPM
  - Current gear
  - Steering angle
  - CPU temperature
- Dark theme optimized for outdoor use
- Landscape orientation for better control

## Configuration

The app connects to the RPi Car backend server. Default configuration:
- Default IP: `192.168.4.1`
- Default Port: `5000`

You can change the server IP in the connection screen.

## Technologies Used

- React Native
- Expo
- TypeScript
- Socket.IO Client
- React Navigation

## Directory Details

### `/android`
Contains the native Android project files. This directory is generated by `expo prebuild` and includes all Android-specific configurations, resources, and build files.

### `/ios`
Contains the native iOS project files. This directory is generated by `expo prebuild` and includes all iOS-specific configurations, resources, and build files.

## Troubleshooting

### Connection Issues
- Ensure the RPi Car backend server is running
- Verify the IP address is correct
- Check that your device is on the same network as the RPi Car

### Build Issues
- Run `npm install` to ensure all dependencies are installed
- Clear cache: `expo start -c`
- Rebuild native projects: `npx expo prebuild --clean`

## License

Same as the parent project.
